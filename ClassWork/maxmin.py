def get_maxmin(list):
	largest = [0]
	smallest = [0]
	newlist = []
	for num in list:
		if num > largest
			largest = num
		if num < smallest 
			smallest = num
		newlist = [largest, smallestr]
	return newlist

print(newlist)